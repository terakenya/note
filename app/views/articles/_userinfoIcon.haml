%ul
  %li
    .c-avator
      .image-class
        -if @article.user.usericon.present?
          = image_tag @user.usericon,class:'commenticon'
        -else
          = image_tag "smile.png",class:'smileicon-30'
  %li
    %p.renewal-c-card__userName
      = article.user.nickname
    %p.renewal-c-card__date
      = article[:created_at].strftime("%Y-%m-%d%H:%M")
      %p-articleNoteInfo__status
        = link_to "削除", article,method: :delete, class: 'delete'
